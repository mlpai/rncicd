import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    container : {
        paddingHorizontal : 8
    },
    shadow : {
        elevation : 15,
        shadowColor : 'red',
    }
})